verif les formulaires pour indiquer si la commande est effectivement traitée ou non
	tous les paramètres sont requis a chaque fois
	vérif les failles (script) insertion de script
	un identifiant d'employé ne doit pas dépasser 16 caractères
	un nouvel id ne doit pas deja exister dans la bd
